-- Drop the duplicate table "timeTable" (with capital T) 
-- Ensure you are keeping "timetable" (lowercase t) which has your ID column and is the standard convention.

DROP TABLE IF EXISTS "timeTable";
